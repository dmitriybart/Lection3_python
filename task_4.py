#Пример создает список и прибавляет 10
# li = [x for x in range(1,21)]
# li = list(map(lambda x: x+10, li))
# print(li)

## Пример принимает строку и делит на числа, преобразуя в лист
data = list(map(int, input.split()))#принимает строку и делит ее на числа, закладывая в лист
